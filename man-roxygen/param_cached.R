#' @param cached boolean whether the cached <%= toupper(data) %> data \code{c<%=data%>} should be returned or new data
#'   should be generated. If set to \code{TRUE} then the other arguments to \code{r<%=data%>} will be ignored.
