#' random.cdisc.data
#'
#' Package to create random SDTM and ADAM datasets.
#'
#' @import magrittr
#' @import lubridate
NULL

# to avoid NOTEs from R CMD CHECK
vars <- c(
  "."
)
for (i in vars) {
  assign(i, NULL)
}

for (i in vars) {
  rm(i)
}
rm(vars)
